 Система логистики "Хром КЗ" (Обновленный)
Цель: Создать полнофункциональный веб-проект на PHP, который служит внутренней системой для отдела логистики компании "Хром КЗ". Система должна включать:

Подачу заявок без авторизации: Клиенты могут оставлять заявки на отгрузку прямо на главной странице.

Отслеживание заказов: Возможность отслеживать заявки по номеру телефона заказчика.

Авторизацию сотрудников: Сотрудники могут регистрироваться и входить в систему.

Персонализированный дашборд: Авторизованные сотрудники могут видеть и управлять своими прошлыми заявками.

📂 1. Структура проекта
index.html: Главная страница, включающая формы входа, регистрации и формы для заявок.

api/: Папка для всех бэкенд-скриптов.

auth_register.php: Эндпоинт для регистрации нового сотрудника.

auth_login.php: Эндпоинт для входа в систему.

new_request.php: Эндпоинт для создания новой заявки на отгрузку.

get_requests.php: Эндпоинт для получения заявок (всех или по номеру телефона).

db.php: Отдельный файл для инициализации подключения к базе данных.

assets/: Папка для статических файлов.

css/:

style.css: Основной файл стилей.

js/:

script.js: Скрипт для работы с фронтендом.

database.sqlite: Файл базы данных SQLite.

💻 2. Бэкенд (PHP) - Логика до мельчайших деталей
2.1. Конфигурация и подключение к БД (api/db.php)
Структура таблиц остается прежней, но в таблице requests поле user_phone теперь будет использоваться для отслеживания, кто подал заявку:

Если заявку подал авторизованный сотрудник, в user_phone будет сохранен его номер.

Если заявку подал клиент без авторизации, поле user_phone будет иметь значение null или пустую строку, а заявка будет привязана к номеру телефона, указанному в поле customer_phone.

2.2. Эндпоинт для заявок (api/new_request.php)
Основное изменение: Этот эндпоинт теперь не требует обязательной авторизации.

Принимает POST-запрос с данными формы, включая номер телефона клиента.

Проверяет сессию: Если пользователь авторизован, сохраняет номер его телефона в поле user_phone. Если нет, то user_phone остается пустым.

Остальная логика (валидация данных, сохранение в БД) остается без изменений.

2.3. Эндпоинт для получения данных (api/get_requests.php)
Изменение в логике: Теперь этот эндпоинт будет работать для отслеживания заказов клиентами.

Принимает GET-запрос с параметром customer_phone.

Возвращает JSON-массив всех заявок, где customer_phone совпадает с переданным значением. Это позволит клиенту видеть все свои заявки, даже если они были поданы без авторизации.

2.4. Эндпоинты аутентификации (api/auth_*.php)
Эти эндпоинты остаются без изменений. Они будут использоваться только для сотрудников, чтобы дать им доступ к более полному функционалу.

🎨 3. Фронтенд (HTML/CSS/JS) - Детальное описание интерфейса
Главная страница (index.html):

В верхней части страницы размещены формы для входа и регистрации (скрыты по умолчанию).

Сразу под ними, видны кнопки "Отгрузки по Астане" и "Отгрузки по Регионам".

Кнопка "Отследить заказ" также видима на главной странице и открывает форму отслеживания.

Все формы (Астана, Регионы, Отслеживание) по-прежнему скрыты и открываются по нажатию на соответствующие кнопки.

Логика JavaScript (assets/js/script.js):

handleNewRequest(formId): Теперь эта функция не проверяет, авторизован ли пользователь, перед отправкой запроса. Она просто собирает данные из формы и отправляет их в api/new_request.php.

handleTracking(): Функция, связанная с кнопкой "Отследить", будет отправлять номер телефона клиента в api/get_requests.php и отображать результат.

fetchMyRequests(): Эта функция будет вызываться только после успешного входа сотрудника. Она отправит запрос в api/get_requests.php с его номером телефона и покажет все заявки, которые он подавал.

🚀 4. Инструкции для Replit
Пожалуйста, создай новый проект на Replit с использованием PHP Web Server. Реализуй все обновленные детали, включая:

Основную страницу, где клиенты могут сразу видеть кнопки для подачи заявок.

Формы заявок, которые не требуют авторизации для отправки.

Функционал отслеживания по номеру телефона заказчика.

Формы входа/регистрации для сотрудников, которые дают доступ к просмотру своих заявок.

Все эндпоинты должны быть реализованы с учетом новых требований.